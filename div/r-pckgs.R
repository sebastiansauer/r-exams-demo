# Benötigte R-Pakete für diesen Kurs


pckgs_exams <-
  c("exams",
    "glue",
    "tidyverse",
    "readxl")


# Führen Sie die folgende Zeile (einmalig!) aus, um die benötigten Pakete zu installieren:
install.packages(pckgs_exams)